# v2.0.1 - Open Source audit alignment

## Changes

- Verified the production Platform API URL and SPA deployment configuration.
- Confirmed the API interceptor does not impose a cold-start timeout.
- Aligned Angular, TypeScript and Angular Material documentation with installed versions.
- Corrected production login links across repository documentation.

## Validation

- `npm ci`
- `npm run build`
- `npm audit --omit=dev`
