services:
  - type: web
    runtime: static
    name: nexa-open-source-webapp
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/nexa-webapp/browser
    envVars:
      - key: NODE_VERSION
        value: "20"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
